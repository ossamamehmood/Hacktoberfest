#include <stdio.h>
int main()
{
    printf("Table Program (Using Loop) \n\n\n");
    printf("Enter A Number That You want the Table : \n");
    int num;
    scanf("%d", &num);
    
    for (int i = 1; i <= 10; i++)
    {
        printf("%d * %d = %d \n", num, i, num * i);
    }
}
