import 'dart:io';

void main() {
  stdout.write("Enter a number :");
  int? n = int.parse(stdin.readLineSync()!);
  print(factorial(n));
}

int factorial(int n) {
  int res = 1, i;
  for (i = 2; i <= n; i++) res *= i;
  return res;
}
