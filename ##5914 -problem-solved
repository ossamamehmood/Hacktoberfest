problem solved #5914
here's the java code
class ListNode {
    int val;
    ListNode next;
    ListNode(int x) {
        val = x;
        next = null;
    }
}

public class LinkedListCycle {
    public boolean hasCycle(ListNode head) {
        if (head == null || head.next == null) {
            return false;
        }
        
        ListNode slow = head;
        ListNode fast = head.next;
        
        while (slow != fast) {
            if (fast == null || fast.next == null) {
                return false; // If fast or fast.next is null, it's not cyclic.
            }
            
            slow = slow.next;
            fast = fast.next.next;
        }
        
        return true; // If we reach here, it means we found a cycle.
    }
}
